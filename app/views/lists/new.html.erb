<h1 >New List</h1>

	<%= form_for @list, remote: true do |f| %>
	<%= f.text_field :title, autofocus: true %>
	<%= submit_tag "Create list" %>
	<% end %>

<%= link_to 'Back', lists_path, class: "btn btn-primary btn-sm"  %>
